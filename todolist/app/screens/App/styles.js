import { StyleSheet } from 'react-native';

import { BLUE, WHITE } from '../../constants/styleConst';

const styles = StyleSheet.create({
  headerStyle: {
    backgroundColor: BLUE
  },
  headerTitleStyle: {
    color: WHITE
  },
  tapBarStyle: {
    backgroundColor: BLUE
  },
  labelStyle: {
    color: WHITE,
    fontSize: 20
  }
});

export default styles;
